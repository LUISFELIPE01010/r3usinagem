import { Link } from "@tanstack/react-router"; import { ChevronRight } from "lucide-react";
type Props={eyebrow:string;title:string;subtitle?:string;image:string;breadcrumb?:{label:string;to?:string}[]};
export function PageHero({eyebrow,title,subtitle,image,breadcrumb}:Props){return <section className="relative isolate flex min-h-[430px] items-end overflow-hidden pt-24"><img src={image} alt="Ambiente industrial da R3 Usinagem" className="absolute inset-0 -z-10 h-full w-full object-cover"/><div className="absolute inset-0 -z-10 bg-gradient-to-r from-carbon via-carbon/85 to-carbon/40"/><div className="container-x w-full pb-14"><span className="eyebrow eyebrow-light">{eyebrow}</span><h1 className="mt-5 max-w-4xl text-5xl text-carbon-foreground sm:text-6xl lg:text-7xl">{title}</h1>{subtitle&&<p className="mt-5 max-w-2xl text-base leading-7 text-carbon-foreground/80">{subtitle}</p>}{breadcrumb&&<nav className="mt-6 flex flex-wrap items-center gap-2 text-[.65rem] uppercase tracking-[.12em] text-carbon-foreground/55" aria-label="Caminho da página">{breadcrumb.map((b,i)=><span key={b.label} className="flex items-center gap-2">{b.to?<Link to={b.to} className="hover:text-brand">{b.label}</Link>:<span className="text-carbon-foreground">{b.label}</span>}{i<breadcrumb.length-1&&<ChevronRight className="h-3 w-3"/>}</span>)}</nav>}</div></section>}
